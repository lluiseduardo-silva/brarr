//! `brarr-cli` — ferramenta de linha de comando para busca em trackers.
//!
//! Subcomando inicial: `brarr search --tmdb <id>` — varre todos os
//! trackers configurados num arquivo `TOML` (URLs + tokens), busca o
//! filme/série pelo ID `TMDB`, mostra releases com scores baseados em
//! regras simples (peso para áudio PT-BR, peso menor para apenas
//! legenda PT-BR, etc.). Logging via `tracing`.
//!
//! Status: stub. Implementação concreta vem na Fase 5.

fn main() {
    // Phase 5 stub — entry point intencionalmente vazio até a CLI ser implementada.
}
